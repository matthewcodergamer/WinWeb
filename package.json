{
  "name": "winweb-xrun",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-coi.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build",
    "upstream:sync": "node scripts/sync-upstreams.mjs",
    "engine:hle:upstream": "node scripts/build-bottleship.mjs",
    "engine:hle:bundle": "node scripts/build-hle-engine.mjs"
  },
  "dependencies": {
    "coi-serviceworker": "^0.1.7"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vite": "^8.0.0"
  }
}
